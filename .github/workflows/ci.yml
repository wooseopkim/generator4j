name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distribution: [oracle, temurin, graalvm, microsoft, corretto]
        java-version: ['21']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java-version }}
          distribution: ${{ matrix.distribution }}
          cache: gradle
      - uses: gradle/actions/setup-gradle@v4
      - run: ./gradlew
      - run: ./gradlew check
      - run: ./gradlew test
